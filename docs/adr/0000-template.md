# ADR NNNN: <title>

## Status

`Proposed` | `Accepted` | `Superseded by ADR NNNN` — YYYY-MM-DD

## Context

What situation, constraint or question forces a decision. State verified facts as facts; reference anything not yet verified by its spike ID in `OPEN_QUESTIONS.md` (S1–S14) instead of asserting it.

## Decision

The decision, in the imperative. Include concrete values (versions, paths, config keys, signatures) so a later reader can check the code against the ADR.

## Consequences

What becomes easier, what becomes harder, what must now be done elsewhere (settings keys, CI jobs, docs, follow-up tasks in `docs/ROADMAP.md`).

## Alternatives considered

One bullet per rejected option: the option, and at least one concrete reason it was rejected.

---

Conventions:

- File name `docs/adr/NNNN-<kebab-slug>.md`; numbers never reused.
- An ADR is never edited after `Accepted` except to change its Status; a new ADR supersedes it.
- Specs link ADRs as `docs/adr/NNNN-<slug>.md`.
